# dependencies
node_modules
.pnp
.pnp.js

# testing
coverage

# next.js
.next
out
build

# production
dist

# misc
.DS_Store
*.pem
.vscode
.idea
.claude

# debug
npm-debug.log*
yarn-debug.log*
yarn-error.log*
.pnpm-debug.log*

# env — never commit anything except .env.example.
# .env.production is on the VPS, NOT in the repo. The deploy.sh script
# uses `git reset --hard` so any accidentally-tracked .env.production
# would be wiped on the next deploy (we learned this the hard way).
.env
.env.local
.env.development.local
.env.test.local
.env.production
.env.production.local
.env.production.bak
.env*.bak
!.env.example

# vercel
.vercel

# typescript
*.tsbuildinfo
next-env.d.ts

# logs
*.log

# supabase
supabase/.branches
supabase/.temp
.github/workflows/
